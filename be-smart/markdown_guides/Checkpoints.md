# all frontend devs will work in the src dir

# make stubs when you need to send data to the server

# dont worry about transforming data too much before you send to middleware endpoints

# when you need to talk to the server import \* as MIDDLE_WARE_NAME from "path/to/middleware.js" - ask elias for help if needed

# in advance tell the backend devs to create end points so we can move fast

# make react components module if possible

# as of right now spend more time on desktop styling but make it so you can easily transform to mobile views

# have centralized styling variables like fonts and colors

# also when you only need to work on just frontend components - use npm run dev (build times takes like 15secs if you do ./run_app.sh)
