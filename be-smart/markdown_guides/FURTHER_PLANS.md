# smart search

    # input to search
    # search btn - must match gig name
    # smart search button - infers

# list of gig results, defaults to rendering all gigs

# have a filter component to cut it down manually
